<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.rokomari">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rokomari">
        <activity
            android:name=".TaskDetailActivity"
            android:exported="true"
            android:label="Task Details"/>
        <activity
            android:name=".TaskAddEditActivity"
            android:exported="true"
            android:label="Add Tasks" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name" />
        <activity
            android:name=".CalenderActivity"
            android:exported="true"
            android:label="Select Deadline" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppThemeForSplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>